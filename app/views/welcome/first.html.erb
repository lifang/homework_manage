<div class="wrap">
  <div class="login_bg">
    <div class="tab" id="login_div" >
      <%= form_tag({:controller => :welcome, :action => :create_first_class}, :remote => true) do %>
       <div class="tab_body clearAfter">
    	<div class="tab_warning">as大三大四的方式的风格豆腐干地方很多返回法国恢复供货</div>
    	<div class="tab_setClass">
          <ul>
            	<li>
                    <div class="select_box">
                        <div class="rowElem">
                            <select id="teaching_material_id" name="teaching_material_id]">
                <option value="0" selected="selected">请选择</option>
                <% @teachering_materials.each do |m| %>
                    <option value="<%= m.id %>"><%= m.name %></option>
                <% end %>
              </select>

                        </div>
                    </div>
                </li>
                <li><input id="class_name" name="class_name" type="text" value="班级名称" /></li>
                <li>
                  
                  <input id="period_of_validity" name="period_of_validity" value="选择结束时间" readonly="readonly"  type="text" onfocus="WdatePicker({dateFmt:'yyyy-MM-dd',autoPickDate:true,minDate:'%y-%M-%d'})" />
                </li>
            </ul>
          <div class="tab_switch_btn"><a href="#">确定</a></div>
        </div>
    </div>
</div>
          <ul>
            <li>
              <p>选择教材</p>
              <select id="teaching_material_id" name="teaching_material_id]">
                <option value="0" selected="selected">请选择</option>
                <% @teachering_materials.each do |m| %>
                    <option value="<%= m.id %>"><%= m.name %></option>
                <% end %>
              </select>
            </li>
            <li>
              <p>输入班级名称</p>
              <input id="class_name" name="class_name" type="text"/>
            </li>
            <li>
              <p>选择结束时间</p>
              <input id="period_of_validity" name="period_of_validity" readonly="readonly"  type="text" onfocus="WdatePicker({dateFmt:'yyyy-MM-dd',autoPickDate:true,minDate:'%y-%M-%d'})" >
            </li>
          </ul>
        </div>
            <input name="teacher_id" value="<%= @teacher.id %>" style="display: none;" type="input">

      </div>
      <div class="tab_btn"><button class="green_btn" type="button" onclick="check_class_info();">确认</button></div>
      <input id="submit_class_info"  style="display: none;" value="确认" type="submit">
      <% end %>
    </div>
  </div>
</div>

<script>
    $(document).ready(function(){
        show_windows("login_div");
    })
</script>