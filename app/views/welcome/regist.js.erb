<% if @info[:status] == true %>
  <% if @from_dictation %>
    setTimeout("location.href = '/welcome/dictation_login';",1000);
  <% else %>
    setTimeout("location.href = '<%= root_url %>';",1000);
  <% end %>

<% else %>
  tishi('<%= @info[:notice] %>');
<% end %>