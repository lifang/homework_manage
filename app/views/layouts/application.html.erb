<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>HomeworkManage</title>
    <%= stylesheet_link_tag    "application", :media => "all"  %>
    <%= stylesheet_link_tag "style.css",'jqtransform.css','jquery.Jcrop.min.css','zuoye.css', :media => "all" %>
    <%= stylesheet_link_tag "skins/square/_all.css",'skins/square/aero.css', 'skins/square/blue.css', 'skins/square/green.css', 'skins/square/grey.css', 'skins/square/orange.css', 'skins/square/pink.css', 'skins/square/purple.css', 'skins/square/red.css', 'skins/square/square.css', 'skins/square/yellow.css', :media => "all" %>
    <%= javascript_include_tag "application", "main_pages", "zuoye","statistics", "main", "main_pages", "icheck", "jquery.tablesorter"  %>
    <%= javascript_include_tag "teachers"%>
    <%= javascript_include_tag "jquery.Jcrop.min"%>
    <%= javascript_include_tag "jquery.jqtransform.js"%>
    <script type="text/javascript" src="/assets/My97DatePicker/WdatePicker.js" ></script>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="wrap">
      <div class="area clearAfter">
        <%=render 'layouts/top_side'%>
        <div class="tab" id="tishi_div" style="display:none; z-index:1011;">
          <span class="close">关闭</span>
          <div class="tab_con">
            <h1 class="red"></h1>
          </div>
        </div>
        <% flash.each do |x,y| %>
          <% if x != "verification_code" %>
            <% break %>
          <% else %>
            <div class="tab flash"  style="display:none;">
              <span class="close">关闭</span>
              <div class="tab_con">
                <h1 class="red"><%=y%></h1>
              </div>
            </div>
          <%end%>
        <%end%>
        <%= yield %>

        <div class="right_side_mess">
          <% if @unread_messes.any? %>
          <a href="javascript:void(0)" class="side_mess_delete" onclick="del_all_unread_msg(<%= @unread_messes[0].user_id %>, <%= @unread_messes[0].school_class_id %>)">全部删除</a>
          <% end %>
          <div class="side_mess_box">
            <ul>
              <% @unread_messes.each do |um| %>
              <a href="/school_classes/<%= @school_class.id %>/messages/check_micropost?uid=<%= um.user_id %>&mid=<%= um.micropost_id %>&mess_id=<%= um.id %>"><li><p><%= um.content.nil? ? "" : um.content.gsub(/\[|\]|;|\||/, "") %></p><p><%= um.created_at.nil? ? "" : um.created_at.strftime("%Y-%m-%d %H:%M") %></p></li></a>
              <% end if @unread_messes.any? %>
            </ul>
          </div>
        </div>

      </div>     
    </div>
    <div class="mask"></div>
    <div class="tab tab840" id="popup_page">

    </div>
  </body>
</html>
